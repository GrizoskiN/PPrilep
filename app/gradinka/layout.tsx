export const metadata = {
  title: "Наша Иднина — CMS",
  robots: { index: false, follow: false },
};

export default function GradinkaStudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
